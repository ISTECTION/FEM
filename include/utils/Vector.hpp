#ifndef _VECTOR_HPP_
#define _VECTOR_HPP_
/// Переделать название - конфликтует

// #include <vector>

// using std::vector;

// vector<vector<double>> &operator*= (vector<vector<double>> &vec, const double _coef) {

//     for (size_t i = 0; i < vec.size(); i++)
//         for (size_t j = 0; j < vec.size(); j++)
//             vec[i][j] *= _coef;

//     return vec;
// }
#endif // _VECTOR_HPP_